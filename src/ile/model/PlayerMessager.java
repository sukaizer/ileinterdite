package ile.model;

public class PlayerMessager extends Player {

    /**
     * Constructeur de classe PlayerMessager
     * @param model
     */
    public PlayerMessager(Model model) {
        super(model);
    }
}
